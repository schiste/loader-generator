{
  "name": "loader-generator",
  "version": "0.1.0",
  "description": "Loader Generator — a browser-based visual generator for animated loading spinners with live preview and CSS / Canvas / SVG export.",
  "license": "AGPL-3.0-or-later",
  "homepage": "https://schiste.github.io/loader-generator/",
  "repository": {
    "type": "git",
    "url": "https://github.com/schiste/loader-generator.git"
  },
  "private": true,
  "scripts": {
    "test": "node --test",
    "serve": "python3 -m http.server 8000",
    "build:css": "npx --yes tailwindcss@3 -c tailwind.config.js -i src/tailwind.css -o tailwind.css --minify"
  }
}
